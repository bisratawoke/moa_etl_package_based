{
  "name": "package-based",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "npx nx run-many --target=build && set NODE_ENV=development && node ./packages/scheduler/dist/index.js"
  },
  "private": true,
  "devDependencies": {
    "@nx/js": "17.2.8",
    "nx": "17.2.8",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
